# Nodestradamus Environment Variables
# Copy this file to .env and fill in your values
# NEVER commit the .env file with real secrets

# =============================================================================
# Embedding Provider Configuration
# =============================================================================

# Embedding provider: "local" (default) or "mistral"
# - local: Uses sentence-transformers, runs on CPU, no API key needed
# - mistral: Uses Codestral Embed API, fast, requires MISTRAL_API_KEY
# NODESTRADAMUS_EMBEDDING_PROVIDER=local

# Mistral API key for Codestral Embed (required if provider=mistral)
# Get yours at: https://console.mistral.ai/api-keys
# Pricing: $0.15 per million tokens
# MISTRAL_API_KEY=your-mistral-api-key-here

# Override embedding model for local provider (optional)
# Default: jinaai/jina-embeddings-v2-base-code
# NODESTRADAMUS_EMBEDDING_MODEL=jinaai/jina-embeddings-v2-base-code

# Parallel API workers for Mistral provider (optional)
# Higher values = faster embedding, but may hit rate limits on some tiers
# Default: 4 (benchmarked: ~3.5x speedup over sequential)
# NODESTRADAMUS_EMBEDDING_WORKERS=4

# =============================================================================
# AI Provider API Keys
# =============================================================================

# DeepSeek API key for AI model access
# Get yours at: https://platform.deepseek.com/
DEEPSEEK_API_KEY=your-deepseek-api-key-here

# OpenAI API key (alternative to DeepSeek)
# OPENAI_API_KEY=sk-your-openai-key-here

# Anthropic API key (for Claude models)
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# =============================================================================
# Optional: GitHub Integration
# =============================================================================

# Personal Access Token for GitHub API (optional, uses GITHUB_TOKEN in Actions)
# Required permissions: repo, workflow
# Create at: https://github.com/settings/tokens
# GITHUB_PAT=ghp_your-personal-access-token-here
